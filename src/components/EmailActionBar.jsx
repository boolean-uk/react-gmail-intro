import '../styles/EmailActionBar.css';

export default function EmailActionBar() {
    return (
        <section className="email-actions">
            <button>Reply</button>
            <button>Forward</button>
        </section>
    )
}